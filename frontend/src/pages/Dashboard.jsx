export default function Dashboard() {
  return (
    <div>
      <h1 className="page-title">Dashboard</h1>
      <p style={{ color: "var(--text-muted)" }}>
        Statistics and charts will appear here (next steps).
      </p>
    </div>
  );
}